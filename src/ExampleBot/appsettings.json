{
  // global settings would go in here. Ones that are safe to be stored in git
  "botname": "My Bot",
  "BonusPoints": {
    "Default": 0
  },
  // you can create appsettings.development.json. We have added this to the .gitignore so it is not checked in. You will see in code where it is used

  // Format for json loading of token - stick this in development version to not need env
  //"Discord": {
  //  "Token": "ABC"
  //}
}